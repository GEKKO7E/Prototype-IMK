
  # DESIGN PROTOTYPE

  This is a code bundle for DESIGN PROTOTYPE. The original project is available at (https://stick-exit-01847023.figma.site/)

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  
